spring:
  datasource:
    url: jdbc:mysql:///tejasdb
    username: root
    password: Tejas@2003
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate.dialect: org.hibernate.dialect.MySQLDialect
